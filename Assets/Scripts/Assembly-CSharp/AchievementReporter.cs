using UnityEngine;

public class AchievementReporter : MonoBehaviour
{
	public TextMesh GPGachievement;

	/*private void Update()
	{
		if (GPGachievement.text != string.Empty)
		{
			Social.ReportProgress(GPGachievement.text, 100.0, delegate
			{
			});
			GPGachievement.text = string.Empty;
		}
	}*/
}
